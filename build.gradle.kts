// Minimal Gradle configuration for GitHub Pages website module (dev)
// This allows the folder to be visible in Android Studio project tree

// No actual build tasks needed - this is just static HTML/JSON files
// that are deployed to GitHub Pages
